apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: test.on.boarding.com
    http:
      paths:
      - path: /web-server
        pathType: Exact
        backend:
          service:
            name: flask-service
            port:
              number: 3307
