apiVersion: v1
kind: Service
metadata:
  name: {{ include "mrx-on-boarding-chart.fullname" . }}
  labels:
    {{- include "mrx-on-boarding-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mrx-on-boarding-chart.selectorLabels" . | nindent 4 }}
