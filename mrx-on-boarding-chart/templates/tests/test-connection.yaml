apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mrx-on-boarding-chart.fullname" . }}-test-connection"
  labels:
    {{- include "mrx-on-boarding-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mrx-on-boarding-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
